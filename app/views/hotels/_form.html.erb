<%= form_for(hotel) do |f| %>
  <% if hotel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hotel.errors.count, "error") %> prohibited this hotel from being saved:</h2>

      <ul>
      <% hotel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <!-- Nested form -->
  <h3>Room Categories</h3>
  <div id="room_categories">
    <%= f.fields_for :room_categories do |hrc| %>
      <%= render 'room_category_fields', :f => hrc %>
    <% end %>
    <div class="links">
      <%= link_to_add_association 'Add Category', f, :room_categories %>
    </div>
  </div>
    <%= f.submit 'Save' %>
<% end %>
